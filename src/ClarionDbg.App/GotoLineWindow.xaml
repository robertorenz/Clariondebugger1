<Window x:Class="ClarionDbg.App.GotoLineWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Go to Line" Width="300" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="NoResize"
        Background="{StaticResource Panel}" Foreground="{StaticResource Fg}" FontFamily="Segoe UI">
    <StackPanel Margin="16">
        <TextBlock x:Name="TxtPrompt" Margin="0,0,0,8"/>
        <TextBox x:Name="TxtLine" Margin="0,0,0,12" Padding="4,3"
                 Background="{StaticResource Bg}" Foreground="{StaticResource Fg}"
                 BorderBrush="{StaticResource Border}" FontFamily="Consolas"
                 KeyDown="TxtLine_KeyDown"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Go" Width="80" Click="Ok_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="80" Click="Cancel_Click" IsCancel="True" Margin="8,0,0,0"/>
        </StackPanel>
    </StackPanel>
</Window>
