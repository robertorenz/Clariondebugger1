<Window x:Class="ClarionDbg.App.LibraryStateWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Library State" Height="560" Width="440" WindowStartupLocation="CenterOwner"
        Background="{StaticResource Panel}" Foreground="{StaticResource Fg}" FontFamily="Segoe UI">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,8">
            <Button DockPanel.Dock="Right" Content="Refresh" Width="80" Click="Refresh_Click"
                    ToolTip="Re-read the stopped thread's RTL state (runs the ClaRUN getters again)"/>
            <TextBlock Text="Per-thread RTL state of the stopped thread" Foreground="{StaticResource FgDim}"
                       FontSize="11" VerticalAlignment="Center"/>
        </DockPanel>

        <TextBlock x:Name="TxtStatus" Grid.Row="1" Margin="0,0,0,8" TextWrapping="Wrap"
                   Foreground="{StaticResource FgDim}" FontSize="11.5" Text="…"/>

        <DataGrid x:Name="Grid" Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  Background="Transparent" Foreground="{StaticResource Fg}" BorderBrush="{StaticResource Border}"
                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="{StaticResource Border}"
                  RowBackground="Transparent" HeadersVisibility="Column" FontFamily="Consolas" FontSize="12.5">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Ok}" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource FgDim}"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.GroupStyle>
                <GroupStyle>
                    <GroupStyle.HeaderTemplate>
                        <DataTemplate>
                            <Border BorderBrush="{StaticResource Border}" BorderThickness="0,0,0,1" Margin="0,6,0,2">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontFamily="Segoe UI"
                                           FontSize="11" Foreground="{StaticResource Accent}" Margin="2,1,0,2"/>
                            </Border>
                        </DataTemplate>
                    </GroupStyle.HeaderTemplate>
                </GroupStyle>
            </DataGrid.GroupStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="130"/>
                <DataGridTextColumn Header="Value" Binding="{Binding Value}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
